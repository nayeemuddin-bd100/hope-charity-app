export enum tagTypes {
  causes = "causes",
  events = "events",
  admin = "admin",
  donations = "donations",
}

export const tagTypesList = [
  tagTypes.causes,
  tagTypes.events,
  tagTypes.admin,
  tagTypes.donations,
];
