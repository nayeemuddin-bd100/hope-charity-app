export enum tagTypes {
  users = "users",
  causes = "causes",
  events = "events",
  admin = "admin",
  donations = "donations",
}

export const tagTypesList = [
  tagTypes.users,
  tagTypes.causes,
  tagTypes.events,
  tagTypes.admin,
  tagTypes.donations,
];
